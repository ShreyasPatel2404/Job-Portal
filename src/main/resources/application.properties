# Spring Mail Configuration (sample, replace with real credentials)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=patelshreyas737@gmail.com
spring.mail.password=${SPRING_MAIL_PASSWORD:vggjxscslzqytkoh}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Application Configuration
spring.application.name=Job-Portal
server.port=8080

# MongoDB Configuration
spring.data.mongodb.uri=mongodb://localhost:27017/jobportal
spring.data.mongodb.auto-index-creation=true

# JWT Configuration (CRITICAL: Set JWT_SECRET environment variable in production)
jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
jwt.expiration=3600000

# Frontend URL Configuration
app.frontend.url=${FRONTEND_URL:http://localhost:3000}

# Email Verification Configuration
app.email.verification.expiry=24
app.password.reset.expiry=1

# File Upload Configuration
app.upload.max-file-size=5242880
app.upload.allowed-types=application/pdf,application/msword,application/vnd.openxmlformats-officedocument.wordprocessingml.document

# Security Configuration
app.security.password.min-length=8
app.security.password.require-uppercase=true
app.security.password.require-lowercase=true
app.security.password.require-digit=true
app.security.password.require-special=true

# Error Messages
user.found=User already registered
user.not.found=User is not registered
invalid.credentials=Invalid credentials
sequence.not.found=Unable to get sequence ID for key: {0}

# CORS Configuration
cors.allowed.origins=http://localhost:3000,http://localhost:3001
